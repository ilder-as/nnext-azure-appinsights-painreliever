# Extracted telemetry — regenerated by the extract-events skill, large, and
# contains real production user data (emails, search terms). NEVER commit.
app/public/data/

# Node / build output
app/node_modules/
app/dist/

# Remembered extraction source (local resource pointer)
.source

# Local design bake-off artifacts
mocks/

# Generated screenshots — embed real prod user data, regenerable from a live run.
docs/*.png

# OS noise
.DS_Store
