# esbuild ships its platform binary as an optional dependency, so its postinstall
# is a no-op for us — but pnpm flags it as a "pending build" and (by default)
# refuses to run scripts until approved. Don't gate `pnpm dev`/`pnpm build` on it.
verify-deps-before-run=false
