/* Product config — this is a generic App Insights analytics tool. Rename freely;
   the live data source (resource) comes from meta.json at runtime. */
export const APP_NAME = "Event Analytics";
export const APP_TAGLINE = "App Insights · custom events";
